import Accelerometer from 'components/widgets/Accelerometer';
import { Layout } from 'components';

export default function AccelerometerPage() {
  return (
    <Layout>
      <Accelerometer />
    </Layout>
  );
}
